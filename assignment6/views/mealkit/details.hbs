<div class="jumbotron p-3 p-md-5 text-white rounded bg-dark" style="background-image: url('{{mealkits.imageUrl}}')">
  <div class="col-md-12 col-lg-6 p-3" style="background-color: rgba(0,0,0,0.6);">
    <h1 class="display-4 font-italic">{{mealkits.title}}</h1>
    <p class="lead mt-3 mb-1">Cooking time: {{mealkits.cookingTime}} minutes</p>
    <p class="lead my-1">Servings: {{mealkits.servings}}</p>
    <p class="lead mt-1 mb-3">Calories: {{mealkits.caloriesPerServing}} /serving</p>
  </div>
</div>
<div class="text-center my-4">
<div><b>Includes:</b> {{mealkits.includes}}</div>
<p class="my-2">{{mealkits.description}}</p>
<div><b>Category:</b> {{mealkits.category}}</div>
</div>

<div class="text-center my-4">
    <div class="btn btn-primary m-auto" onclick="addToCart(event)" data-key={{mealkits._id}}>Buy for {{mealkits.price}}</div>
</div>
